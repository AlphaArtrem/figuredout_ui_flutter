# Dart / Flutter
.dart_tool/
.packages
build/
pubspec.lock
.flutter-plugins
.flutter-plugins-dependencies

# Generated sources. Widgetbook's directories.g.dart is regenerated by CI
# before `flutter build web` — see AGENTS.md §Validation (plan G9).
*.g.dart
*.freezed.dart
*.mocks.dart

# Local dependency overrides — never commit a machine-local path.
pubspec_overrides.yaml

# Editors / OS
.idea/
*.iml
.vscode/
.DS_Store
